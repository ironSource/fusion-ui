// The values provided below are different from the defaults (see: https://prettier.io/docs/en/options.html).
// If you don't specify one of these properties,
// the default value will be applied.

module.exports = {
    printWidth: 140,
    tabWidth: 4, // 2
    singleQuote: true, // false
    bracketSpacing: false, // true
    trailingComma: "none",
    arrowParens: "avoid"
};
