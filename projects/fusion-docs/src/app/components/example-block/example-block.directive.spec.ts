import {ExampleBlockDirective} from './example-block.directive';

describe('ExampleBlockDirective', () => {
    it('should create an instance', () => {
        const directive = new ExampleBlockDirective(null);
        expect(directive).toBeTruthy();
    });
});
