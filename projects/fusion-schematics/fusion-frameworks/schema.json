{
    "$schema": "http://json-schema.org/schema",
    "id": "FusionSchematics",
    "title": "Fusion Schematics",
    "type": "object",
    "description": "Fusion Frameworks",
    "properties": {
        "componentsFilePath": {
            "type": "string",
            "description": "the relative path of the exported components typescript file",
            "x-prompt": "What is the components file path?"
        },
        "outputDir": {
            "type": "string",
            "description": "the path for output frameworks wrapper directory",
            "x-prompt": "What should be output directory?"
        }
    },
    "required": ["componentsFilePath", "outputDir"]
}
