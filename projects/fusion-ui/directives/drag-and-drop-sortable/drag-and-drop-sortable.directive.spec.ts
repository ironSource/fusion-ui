import {DragAndDropSortableDirective} from './drag-and-drop-sortable.directive';

describe('DragAndDropSortableDirective', () => {
    it('should create an instance', () => {
        const directive = new DragAndDropSortableDirective(null, null);
        expect(directive).toBeTruthy();
    });
});
