import {ScrollToDirective} from './scroll-to.directive';

describe('ScrollToDirective', () => {
    it('should create an instance', () => {
        const directive = new ScrollToDirective(null);
        expect(directive).toBeTruthy();
    });
});
