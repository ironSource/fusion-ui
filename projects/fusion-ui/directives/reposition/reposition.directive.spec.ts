import {RepositionDirective} from "./reposition.directive";

describe('RepositionDirective', () => {
    it('should create an instance', () => {
        const directive = new RepositionDirective(null);
        expect(directive).toBeTruthy();
    });
});
