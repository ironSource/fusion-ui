import {IntersectionDirective} from './intersection.directive';

describe('IntersectionDirective', () => {
    it('should create an instance', () => {
        const directive = new IntersectionDirective(null, null);
        expect(directive).toBeTruthy();
    });
});
