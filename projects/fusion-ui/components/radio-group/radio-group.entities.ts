import {IconData} from '@ironsource/fusion-ui/components';

export interface RadioGroupOptions {
    id: string;
    label: string;
    icon?: IconData;
    tooltip?: string;
    disabled?: boolean;
}
