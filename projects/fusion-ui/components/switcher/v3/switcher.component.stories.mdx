import {Canvas, Meta, Story, Description, Source, ArgsTable} from '@storybook/addon-docs';
import {dedent} from 'ts-dedent';
import {SwitcherComponent} from './switcher.component';

<Meta title="Components/Inputs/Switch/All"/>

<Description markdown={dedent`
# Switch
Switches are the preferred way to adjust settings on mobile. The option that the switch controls, as well as the state it's in, should be made clear from the corresponding inline label.
`}/>

## Usage
<Source language="html"
        code={dedent`
<fusion-switcher
    [configuration]="configuration"
    [options]="options"
    [formControl]="formControl"
></fusion-switcher>
`}/>

### Default
<Canvas>
    <Story id="components-inputs-switch--default"></Story>
</Canvas>

### Large
<Canvas>
    <Story id="components-inputs-switch--large"></Story>
</Canvas>


<ArgsTable of={SwitcherComponent}/>
