import {Meta, IconGallery, IconItem} from '@storybook/addon-docs';
import {assetsPath, iconsBrandedData, iconsADUnitData, iconsABTestData, iconsAdNetworks} from "./iconsData";

<Meta title="V4/Foundation/Icons"/>

<style>
    {
        `
.sbdocs-title{
    margin-bottom: 8px
    color: var(--text-primary, hsl(0,0%,12.5%));
    font-family: Inter;
    font-size: 40px;
    font-style: normal;
    font-weight: 600;
    line-height: 48px; /* 120% */
    letter-spacing: -0.88px;
}
.subtitle-border-bottom{
    border-bottom: 1px solid var(--common-divider, hsl(0, 0%, 86.8%));
    padding-bottom: 8px;
    margin: 32px 0;
    color: var(--text-primary, hsl(0,0%,12.5%));
    font-family: Inter;
    font-size: 24px;
    font-style: normal;
    font-weight: 600;
    line-height: 28px; /* 116.667% */
    letter-spacing: -0.456px;
}
.icons-title{
    color: var(--text-secondary, #646464);
    font-family: Inter;
    font-size: 14px;
    font-style: normal;
    font-weight: 500;
    line-height: 20px;
    letter-spacing: -0.084px;
    margin-bottom: 8px;
}
.icons-wrapper {
    margin-bottom: 32px;
    padding: 32px;
    border: 1px solid var(--common-divider, hsl(0, 0%, 86.8%));
}
.icons-wrapper-notes{
    margin-top: -10px;
    padding-top: 14px;
    color: var(--text-primary, #202020);
    font-family: Inter;
    font-size: 13px;
    font-style: normal;
    font-weight: 400;
    line-height: 20px;
    letter-spacing: -0.039px;
    border-top: 1px solid var(--common-divider, hsl(0, 0%, 86.8%));
}
.icons-ab-test-wrapper .css-15erloe {
    width: 54px;
    height: 54px;
}
a {
    color: #029CFD;
    text-decoration: none;
}
p {
    margin: 16px 0;
    color: var(--text-secondary, #646464);
    font-family: Inter;
    font-size: 14px;
    font-style: normal;
    font-weight: 400;
    line-height: 26px
}
.text-block{
    color: var(--text-primary, #202020);
    font-family: Inter;
    font-size: 14px;
    font-style: normal;
    font-weight: 400;
    line-height: 20px;
    letter-spacing: -0.084px;
    margin-bottom: var(--spacing-100, 8px);
}
code {
    display: inline-table;
    line-height: 20px;
    margin: 0 2px;
    padding: 2px 4px;
    white-space: nowrap;
    border-radius: 4px;
    font-size: 14px;
    letter-spacing: -0.072px;
    color: var(--text-primary, #202020);
    background: var(--background-paper-elevation-2, #EBEBEB);
}
h4{
    margin-bottom: 10px;
}
ul{
    margin-left: 16px;
    padding-left: 8px;
    font-family: Inter;
    font-size: 14px;
    font-style: normal;
    font-weight: 400;
    line-height: 20px;
    letter-spacing: -0.084px;
}
ul li{
    margin: 5px 0;
    color: var(--text-primary, #202020);
}
.info-block{
    margin: 32px 0;
    display: flex;
    padding: var(--spacing-300, 24px);
    flex-direction: column;
    align-items: flex-start;
    gap: 4px;
    align-self: stretch;
    border-radius: 4px;
    background: var(--background-paper-elevation-0, #F9F9F9);
}
.info-block h4 {
    color: var(--text-secondary, #646464);
    font-family: Inter;
    font-size: 14px;
    font-style: normal;
    font-weight: 600;
    line-height: 20px;
    letter-spacing: -0.084px;
    margin-bottom: 0;
}
.icons-ad-networks {
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    gap: 32px;
}
.icon-ad-network{
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 4px;
    align-self: stretch;
    width: 31%;
}
.ad-network{
    width: 100%;
    border-bottom: 1px solid var(--common-divider, #E4E4E4);
}
.ad-network-name{
    color: var(--text-secondary, #646464);
    font-family: Inter;
    font-size: 7px;
    font-style: normal;
    font-weight: 400;
    line-height: normal;
    cursor: default;
}
`
    }
</style>
<h1 class="sbdocs-title">Icons</h1>
When used correctly, UI icons can speed up user interactions by being immediately recognizable, and they can also add style and visual interest to your user interface designs.

<h1 class="subtitle-border-bottom">Usage</h1>
<div class="text-block">Put ```import {IconModule} from '@ironsource/fusion-ui/components/icon/v1';``` in your component / module script block</div>
<div class="text-block">Add it to your component (if it standalone component) or module import  ```imports: [CommonModule, IconModule],```</div>
<div class="text-block">and ```<fusion-icon [name]="iconName"></fusion-icon>``` in your components template.</div>

<h1 class="subtitle-border-bottom">Props</h1>
- name (string) - icon name with icon type as prefix of name property (e.g. 'ph/regular/arrow-left' or 'v4/branded/ironsource');
    - types('ph/regular' | 'ph/bold'  | 'ph/fill' | 'v4/branded' | 'v4/ad-unit' | 'v4ab-test');
- size you can set as style property
- color icon get as currentColor from wrapper component


<h1 class="subtitle-border-bottom">Stories</h1>


<div class="icons-title">Phosphor</div>
<div class="icons-wrapper">
    <div style={{display: 'flex', alignItems: 'center', gap: '12px', marginBottom: '14px'}}>
        <img src="https://fusion.ironsrc.net/assets/images/v4/phosphor-logo.svg" alt="Phosphor" style={{width: '32px', height: '32px'}}/>
        <div>**<a href="https://phosphoricons.com/" target="_blank">View in Phosphor</a>**</div>
    </div>
    <div style={{display: 'flex', alignItems: 'center', gap: '12px'}}>
        <img src="https://fusion.ironsrc.net/assets/images/v4/figma-logo.svg" alt="Figma" style={{width: '32px', height: '32px'}}/>
        <div>**<a href="https://www.figma.com/community/file/903830135544202908/phosphor-icons" target="_blank">View in Figma</a>**</div>
    </div>
</div>

<div class="text-block">In additional to the icons provided by Phosphor, we also provide a set of icons that are commonly used.</div>
<br/>
<div class="icons-title">Brand</div>
<div class="icons-wrapper">
    <IconGallery style={{marginTop: '24px'}}>
        {
            iconsBrandedData.map(icon=>{
                return (
                    <IconItem name={icon.name} key={icon.name}>
                        <img src={assetsPath + icon.path} alt={icon.name} style={{width: '24px', height: '24px'}}/>
                    </IconItem>
                )
            })
        }
    </IconGallery>
    <div class="icons-wrapper-notes">icon type: "branded"</div>
</div>

<div class="icons-title">Ad Unit</div>
<div class="icons-wrapper">
    <IconGallery style={{marginTop: '24px'}}>
        {
            iconsADUnitData.map(icon=>{
                return (
                    <IconItem name={icon.name} key={icon.name}>
                        <img src={assetsPath + icon.path} alt={icon.name} style={{width: '24px', height: '24px'}}/>
                    </IconItem>
                )
            })
        }
    </IconGallery>
    <div class="icons-wrapper-notes">icon type: "ad-unit"</div>
</div>

<div class="icons-title">A/B test</div>
<div class="icons-wrapper icons-ab-test-wrapper">
    <IconGallery style={{marginTop: '24px'}}>
        {
            iconsABTestData.map(icon=>{
                return (
                    <IconItem name={icon.name} key={icon.name}>
                        <img src={assetsPath + icon.path} alt={icon.name} style={{width: '38px', height: '38px'}}/>
                    </IconItem>
                )
            })
        }
    </IconGallery>
    <div class="icons-wrapper-notes">icon type: "ab-test"</div>
</div>

<div class="icons-title">Ad networks</div>
<div class="icons-wrapper">
    <div class="icons-ad-networks">
        {
            iconsAdNetworks.map(icon=>{
                return (
                    <div class="icon-ad-network">
                        <div class="ad-network">
                            <img src={assetsPath + icon.path} title={icon.path} alt={icon.name} style={{height: '16px'}}/>
                        </div>
                        <div class="ad-network-name" title={icon.name}>{icon.name}</div>
                    </div>
                )
            })
        }
    </div>
    <div class="icons-wrapper-notes" style={{'border-top': 'none', 'padding-top': '26px'}}>icon type: "ad-networks"</div>
</div>

<div class="icons-title">Ad networks (flat)</div>
<div class="icons-wrapper">
    <div class="icons-ad-networks">
        {
            iconsAdNetworks.map(icon=>{
                return (
                    icon.path = icon.path.replace('ad-networks/', 'ad-networks/flat/'),
                    <div class="icon-ad-network">
                        <div class="ad-network">
                            <img src={assetsPath + icon.path} title={icon.path} alt={icon.name} style={{height: '16px'}}/>
                        </div>
                        <div class="ad-network-name" title={icon.name}>{icon.name}</div>
                    </div>
                )
            })
        }
    </div>
    <div class="icons-wrapper-notes" style={{'border-top': 'none', 'padding-top': '26px'}}>icon type: "ad-networks/flat"</div>
</div>