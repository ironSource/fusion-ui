import {GetObjectLengthPipe} from './get-object-length.pipe';

describe('FilterByFieldPipe', () => {
    it('create an instance', () => {
        const pipe = new GetObjectLengthPipe();
        expect(pipe).toBeTruthy();
    });
});
